x-main-image: &main-image
  image: ${MEMEXPERT_MAIN_IMAGE:-memexpert-main:e2e}
  build:
    context: .
    dockerfile: Dockerfile
    target: main

x-worker-image: &worker-image
  image: ${MEMEXPERT_WORKER_IMAGE:-memexpert-worker:e2e}
  build:
    context: .
    dockerfile: Dockerfile
    target: worker

x-app-env: &app-env
  APP_HOST: 0.0.0.0
  APP_PORT: 8000
  DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-memexpert}:${POSTGRES_PASSWORD:-memexpert}@postgres:5432/${POSTGRES_DB:-memexpert}
  REDIS_URL: redis://redis:6379/0
  RABBITMQ_URL: amqp://${RABBITMQ_DEFAULT_USER:-memexpert}:${RABBITMQ_DEFAULT_PASS:-memexpert}@rabbitmq:5672/
  QDRANT_URL: http://qdrant:6333
  MEILISEARCH_URL: http://meilisearch:7700
  MEILISEARCH_MASTER_KEY: ${MEILISEARCH_MASTER_KEY:-memexpert-e2e-meili-key}
  S3_ENDPOINT: http://minio:9000
  S3_ACCESS_KEY: ${S3_ACCESS_KEY:-memexpert}
  S3_SECRET_KEY: ${S3_SECRET_KEY:-memexpert-secret}
  S3_BUCKET: ${S3_BUCKET:-memexpert-e2e}
  S3_REGION: ${S3_REGION:-us-east-1}
  IMGPROXY_BASE_URL: http://imgproxy:8080
  MEDIA_PUBLIC_BASE_URL: ""
  PIPELINE_OPERATOR_TOKEN: ${PIPELINE_OPERATOR_TOKEN:-memexpert-e2e-pipeline-operator-token-min-32}
  PIPELINE_ALLOWED_MIME_TYPES: image/jpeg,image/png,image/webp,image/gif,video/mp4,video/quicktime,video/webm
  PIPELINE_BROKER_CONNECTION_TIMEOUT_SECONDS: "10.0"
  PIPELINE_STORAGE_CONNECTION_TIMEOUT_SECONDS: "10.0"
  PIPELINE_OCR_PROVIDER_MODE: fake
  PIPELINE_VOYAGE_PROVIDER_MODE: fake
  PIPELINE_CLASSIFICATION_PROVIDER_MODE: fake
  PIPELINE_VOYAGE_OUTPUT_DIMENSIONS: "4"
  PIPELINE_QDRANT_COLLECTION_NAME: memexpert-memes-e2e
  PIPELINE_MEILISEARCH_INDEX_NAME: memexpert-memes-e2e
  RECOMMENDATION_ENABLED: "true"
  RECOMMENDATION_SHADOW_MODE: "false"
  RECOMMENDATION_CANARY_PERCENT: "100"
  PIPELINE_WORKER_PREFETCH_COUNT: "1"
  RUNTIME_HEALTH_FILE: /tmp/memexpert-runtime-health.json
  RUNTIME_HEALTH_INTERVAL_SECONDS: "1.0"
  RUNTIME_HEALTH_STALE_AFTER_SECONDS: "10.0"
  RUNTIME_HEALTH_OPERATION_TIMEOUT_SECONDS: "120.0"
  DATABASE_POOL_TIMEOUT_SECONDS: "5.0"
  AUTH_JWT_SECRET: ${AUTH_JWT_SECRET:-memexpert-e2e-jwt-secret-with-32-byte-minimum}
  AUTH_ACCESS_COOKIE_SECURE: "false"
  SECURITY_CORS_ALLOWED_ORIGINS: http://frontend:3000,http://localhost:3000,http://127.0.0.1:3000
  SECURITY_RATE_LIMIT_ENABLED: "false"
  TELEGRAM_SESSION_ENCRYPTION_SECRET: ${TELEGRAM_SESSION_ENCRYPTION_SECRET:-memexpert-e2e-telegram-session-encryption-secret}

services:
  postgres:
    image: postgres:16.14
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-memexpert}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-memexpert}
      POSTGRES_DB: ${POSTGRES_DB:-memexpert}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  redis:
    image: redis:7.4.9
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s

  rabbitmq:
    image: rabbitmq:4.3.1-management
    user: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER:-memexpert}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS:-memexpert}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 10s
      retries: 20
      start_period: 20s

  qdrant:
    image: qdrant/qdrant:v1.18.3
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -lc 'exec 3<>/dev/tcp/127.0.0.1/6333 && printf \"GET /readyz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3 && grep -q \"200 OK\" <&3'",
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  meilisearch:
    image: getmeili/meilisearch:v1.46.1
    environment:
      MEILI_MASTER_KEY: ${MEILISEARCH_MASTER_KEY:-memexpert-e2e-meili-key}
      MEILI_ENV: development
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - meili_data:/meili_data
    healthcheck:
      test: ["CMD-SHELL", "wget --spider --quiet http://127.0.0.1:7700/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-memexpert}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-memexpert-secret}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9000/minio/health/live || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  minio-init:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    entrypoint: [""]
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-memexpert}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-memexpert-secret}
      S3_BUCKET: ${S3_BUCKET:-memexpert-e2e}
    command:
      [
        "/bin/sh",
        "-c",
        "mc alias set memexpert http://minio:9000 \"$${MINIO_ROOT_USER}\" \"$${MINIO_ROOT_PASSWORD}\" && mc mb --ignore-existing \"memexpert/$${S3_BUCKET}\"",
      ]

  imgproxy:
    image: darthsim/imgproxy:v4.0.4
    environment:
      IMGPROXY_USE_S3: "true"
      IMGPROXY_S3_ENDPOINT: http://minio:9000
      IMGPROXY_S3_REGION: ${S3_REGION:-us-east-1}
      IMGPROXY_S3_USE_PATH_STYLE: "true"
      AWS_ACCESS_KEY_ID: ${S3_ACCESS_KEY:-memexpert}
      AWS_SECRET_ACCESS_KEY: ${S3_SECRET_KEY:-memexpert-secret}
      IMGPROXY_BIND: ":8080"
    depends_on:
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "imgproxy", "health"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  migrate:
    <<: *main-image
    restart: "no"
    command: ["alembic", "upgrade", "head"]
    environment: *app-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
      imgproxy:
        condition: service_healthy
    healthcheck:
      disable: true

  api:
    <<: *main-image
    environment: *app-env
    depends_on:
      migrate:
        condition: service_completed_successfully

  worker-media:
    <<: *worker-image
    command: ["memexpert-workers", "--role", "media"]
    environment:
      <<: *app-env
      PIPELINE_WORKER_PREFETCH_COUNT: "1"
      DATABASE_POOL_SIZE: "2"
      DATABASE_MAX_OVERFLOW: "1"
      DATABASE_APPLICATION_NAME: memexpert-e2e-worker-media
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "memexpert-runtime-health"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 20s

  worker-ocr:
    <<: *worker-image
    command: ["memexpert-workers", "--role", "ocr"]
    environment:
      <<: *app-env
      PIPELINE_WORKER_PREFETCH_COUNT: "1"
      DATABASE_POOL_SIZE: "1"
      DATABASE_MAX_OVERFLOW: "1"
      DATABASE_APPLICATION_NAME: memexpert-e2e-worker-ocr
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "memexpert-runtime-health"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 30s

  worker-enrichment:
    <<: *worker-image
    command: ["memexpert-workers", "--role", "enrichment"]
    environment:
      <<: *app-env
      PIPELINE_WORKER_PREFETCH_COUNT: "2"
      DATABASE_POOL_SIZE: "3"
      DATABASE_MAX_OVERFLOW: "1"
      DATABASE_APPLICATION_NAME: memexpert-e2e-worker-enrichment
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "memexpert-runtime-health"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 20s

  worker-sync:
    <<: *worker-image
    command: ["memexpert-workers", "--role", "sync"]
    environment:
      <<: *app-env
      PIPELINE_WORKER_PREFETCH_COUNT: "4"
      DATABASE_POOL_SIZE: "3"
      DATABASE_MAX_OVERFLOW: "1"
      DATABASE_APPLICATION_NAME: memexpert-e2e-worker-sync
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "memexpert-runtime-health"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 20s

  worker-telegram:
    <<: *worker-image
    command: ["memexpert-workers", "--role", "telegram"]
    environment:
      <<: *app-env
      PIPELINE_WORKER_PREFETCH_COUNT: "1"
      DATABASE_POOL_SIZE: "2"
      DATABASE_MAX_OVERFLOW: "1"
      DATABASE_APPLICATION_NAME: memexpert-e2e-worker-telegram
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "memexpert-runtime-health"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 20s

  frontend:
    image: ${MEMEXPERT_FRONTEND_IMAGE:-memexpert-frontend:e2e}
    build:
      context: .
      dockerfile: frontend/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      ORIGIN: http://frontend:3000
      API_BASE_URL: http://api:8000
    depends_on:
      api:
        condition: service_healthy

  seed:
    <<: *worker-image
    restart: "no"
    command:
      [
        "python",
        "-m",
        "scripts.seed_e2e",
        "--api-base-url",
        "http://api:8000",
        "--artifacts-dir",
        "/artifacts",
        "--run-id",
        "${E2E_RUN_ID:-local}",
      ]
    environment: *app-env
    depends_on:
      api:
        condition: service_healthy
      worker-media:
        # The orchestrator separately gates seed on role ownership for all seven RabbitMQ consumers.
        condition: service_started
      worker-ocr:
        condition: service_started
      worker-enrichment:
        condition: service_started
      worker-sync:
        condition: service_started
      worker-telegram:
        condition: service_started
    volumes:
      - type: bind
        source: ${E2E_ARTIFACT_DIR:?set E2E_ARTIFACT_DIR to an absolute per-run artifact directory}
        target: /artifacts

  e2e-runner:
    image: ${MEMEXPERT_E2E_RUNNER_IMAGE:-memexpert-e2e-runner:e2e}
    build:
      context: .
      dockerfile: e2e/Dockerfile
    restart: "no"
    environment:
      CI: "1"
      E2E_RUN_ID: ${E2E_RUN_ID:-local}
      E2E_ARTIFACTS_DIR: /artifacts
      E2E_SEED_FILE: /artifacts/seed.json
      E2E_FRONTEND_BASE_URL: http://frontend:3000
      E2E_API_BASE_URL: http://api:8000
      E2E_OPERATOR_TOKEN: ${PIPELINE_OPERATOR_TOKEN:-memexpert-e2e-pipeline-operator-token-min-32}
    depends_on:
      frontend:
        condition: service_healthy
      seed:
        condition: service_completed_successfully
    volumes:
      - type: bind
        source: ${E2E_ARTIFACT_DIR:?set E2E_ARTIFACT_DIR to an absolute per-run artifact directory}
        target: /artifacts

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  qdrant_data:
  meili_data:
  minio_data:
